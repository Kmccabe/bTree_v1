import React from 'react';

export default function Landing(): JSX.Element {
  return (
    <main style={{ padding: '2rem' }}>
      <h1>Landing</h1>
      <p>TODO: hero + CTAs (Join as Subject, Run as Experimenter).</p>
    </main>
  );
}
