[project]
name = "btree-registry-contracts"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pyteal>=0.24",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
